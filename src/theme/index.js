// ResumeForge AI — Design Tokens v2
// Brand direction: Techy & AI-forward
// Primary ramp: Electric blue  |  Accent: Cyan  |  Forge: Ember orange

export const colors = {
  // Brand — primary electric blue
  primary:       '#1D4ED8',   // electric blue — buttons, links, key actions
  primaryDark:   '#1E3A8A',   // deep navy blue — pressed states
  primaryLight:  '#EFF6FF',   // sky tint — backgrounds, selected states

  // Brand — AI accent cyan
  accent:        '#06B6D4',   // cyan — highlights, AI-related elements, badges
  accentLight:   '#ECFEFF',   // pale cyan — accent backgrounds

  // Brand — forge ember (used sparingly for warmth / energy)
  ember:         '#EA580C',   // orange ember — CTAs, match score highlights
  emberLight:    '#FFF7ED',   // warm tint — ember backgrounds

  // Neutrals (blue-tinted for a techy feel)
  background:    '#F8FAFF',   // barely-blue white — screen backgrounds
  surface:       '#FFFFFF',   // pure white — cards, inputs, modals
  border:        '#E2E8F0',   // cool gray — dividers, input borders
  textPrimary:   '#0F172A',   // deep navy black — headings, body
  textSecondary: '#475569',   // slate — labels, secondary info
  textMuted:     '#94A3B8',   // light slate — placeholders, hints

  // Feedback
  success:       '#10B981',   // green — success states
  warning:       '#F59E0B',   // amber — warnings
  error:         '#EF4444',   // red — errors
  errorLight:    '#FEF2F2',   // light red — error backgrounds
};

export const spacing = {
  xs:  4,
  sm:  8,
  md:  16,
  lg:  24,
  xl:  32,
  xxl: 48,
};

export const font = {
  sm:  14,
  md:  16,
  lg:  18,
  xl:  22,
  xxl: 28,
};

export const radius = {
  sm:   6,
  md:   12,
  lg:   16,
  full: 9999,
};
