// ResumeForge AI — Design Tokens
// Placeholder palette — swap these out when your designer is ready

export const colors = {
  // Brand
  primary: '#4F46E5',       // indigo — buttons, links, accents
  primaryDark: '#3730A3',   // darker indigo — pressed states
  primaryLight: '#EEF2FF',  // light indigo — backgrounds, badges

  // Neutrals
  background: '#F9FAFB',    // off-white — screen backgrounds
  surface: '#FFFFFF',       // pure white — cards, modals
  border: '#E5E7EB',        // light gray — dividers, input borders
  textPrimary: '#111827',   // near-black — headings, body
  textSecondary: '#6B7280', // medium gray — labels, hints
  textMuted: '#9CA3AF',     // light gray — placeholders

  // Feedback
  success: '#10B981',       // green — success states
  warning: '#F59E0B',       // amber — warnings
  error: '#EF4444',         // red — errors
  errorLight: '#FEF2F2',    // light red — error backgrounds
};

export const spacing = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
};

export const font = {
  sm: 14,
  md: 16,
  lg: 18,
  xl: 22,
  xxl: 28,
};

export const radius = {
  sm: 6,
  md: 12,
  lg: 16,
  full: 9999,
};
